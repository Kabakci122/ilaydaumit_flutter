
import 'package:flutter/material.dart';

class ChatScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("İlayda ile Sohbet")),
      body: Center(child: Text("Sohbet başlasın 💬❤️")),
    );
  }
}
